name: GCP Deploy

on:
  workflow_call:
    inputs:
      image_name:
        description: Docker image name (e.g. sunrise-sunset-calendar) — pulled from docker.io/mbologna/{image_name}
        required: true
        type: string
      deploy_type:
        description: "Cloud Run resource type: 'service' or 'job'"
        required: false
        type: string
        default: service
      deploy_name:
        description: Cloud Run service or job name to deploy
        required: true
        type: string
      region:
        description: GCP region
        required: false
        type: string
        default: us-central1
    secrets:
      GCP_WORKLOAD_IDENTITY_PROVIDER:
        required: true
      GCP_SERVICE_ACCOUNT:
        required: true

# WIF requires id-token: write.
permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Validate deploy_type
        env:
          DEPLOY_TYPE: ${{ inputs.deploy_type }}
        run: |
          if [[ "$DEPLOY_TYPE" != "service" && "$DEPLOY_TYPE" != "job" ]]; then
            echo "::error::deploy_type must be 'service' or 'job', got '$DEPLOY_TYPE'"
            exit 1
          fi

      - name: Compute short SHA
        id: sha
        run: echo "short=${GITHUB_SHA:0:7}" >> "$GITHUB_OUTPUT"

      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - uses: google-github-actions/setup-gcloud@aa5489c8933f4cc7a4f7d45035b3b1440c9c10db # v3

      - name: Deploy Cloud Run Service
        if: inputs.deploy_type == 'service'
        uses: google-github-actions/deploy-cloudrun@2028e2d7d30a78c6910e0632e48dd561b064884d # v3
        with:
          service: ${{ inputs.deploy_name }}
          image: docker.io/mbologna/${{ inputs.image_name }}:sha-${{ steps.sha.outputs.short }}
          region: ${{ inputs.region }}

      - name: Deploy Cloud Run Job
        if: inputs.deploy_type == 'job'
        env:
          DEPLOY_NAME: ${{ inputs.deploy_name }}
          IMAGE_NAME: ${{ inputs.image_name }}
          SHORT_SHA: ${{ steps.sha.outputs.short }}
          REGION: ${{ inputs.region }}
        run: |
          gcloud run jobs deploy "$DEPLOY_NAME" \
            --image "docker.io/mbologna/$IMAGE_NAME:sha-$SHORT_SHA" \
            --region "$REGION" \
            --quiet
